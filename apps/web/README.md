# apps/web

Cliente canônico. PWA. React (hub, meta) + PixiJS (batalha e Spire).

Ainda não há implementação — pré-produção. Contratos:

- fala com a API só por URLs relativas;
- usa `@relicwake/sim` só para previsão/replay;
- nunca credita moeda.
