# Áudio

Pasta reservada a stems (hub, batalha, SFX, VO) no content complete.

O slice usa síntese em `apps/web/src/audio.ts` — não há WAV/OGG obrigatório no first paint. Quando os loops de 1.0 existirem, o mesmo `setBed` / `playSfx` passa a apontar para estes arquivos.
