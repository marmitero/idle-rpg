# @relicwake/sim

Função pura de combate. Mesmo binário no servidor (autoridade) e no cliente (previsão).

```
simulate(loadoutA, loadoutB, directives, seed, content) → BattleResult
```

Sem I/O. Sem `Math.random`. Golden replays no CI.
